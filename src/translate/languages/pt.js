const messages = {
    pt: {
        translations: {
            // [pageName]: {
                // [key]: [value]
                // [key]: [value]
                // [key]: [value]
                // ...
            // }
        }
    }
};

export { messages };

